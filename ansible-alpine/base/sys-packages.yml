---
- name: Install sys base packages
  become: yes
  community.general.apk:
    name:
      - ack
      - bc
      - curl
      - git
      - go
      # - helm
      - jq
      - tmux
      # - kubectl
      - vim
      - wget
    update_cache: true
